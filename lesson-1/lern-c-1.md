1# how to run c on Windows/Linux

Windows:
so for run c in windons u have visual studio community, just need to go website of Microsoft and install the vs community after install on wedsite u will need make sure that u did tick on c++ dev in that one we have what we need to run c

next make project and name that somehow
, make file "main.c"

Linux:
and for Linux u not need to install nothing u will need only terminal or bash and one command


main.c:
#include <stdio.h> <-This is make the pc understand c but in terminal

int main() <- int is integer the mean is whole 
            - main is main of the start the function
            -() in which the parameters are specified
{           -{} start cod and the exit

    printf("Hello, World!\n"); <-  printf("") is print in terminal that we want to see
                                - \n is t
    getchar(); <- getchar(); is that one make terminal will be and not exit only when we ender

    return 0; <- return 0; is make finish that file or main
}